package mk.ukim.finki.aibotbackend.model.enums;

/**
 * The social networks a bot implementation can target.
 * Each student implements the bot for exactly ONE of these
 * (assigned by the professor).
 */
public enum SocialNetwork {
    FACEBOOK,
    INSTAGRAM,
    X,
    REDDIT,
    TIKTOK,
    YOUTUBE,
    THREADS,
    LINKEDIN,
    /** gol.mk — Macedonian sports portal (sports variant of the assignment). */
    SPORTS_PORTAL_GOL
}
